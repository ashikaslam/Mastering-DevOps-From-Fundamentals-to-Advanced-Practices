# =============================================================================
# CI/CD Pipeline — dev environment
# Trigger : manual (workflow_dispatch)
# Actions : lint → build images → push to registry → deploy to k8s/dev
# =============================================================================
name: CI/CD — Dev

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual deployment"
        required: false
        default: "Manual dev deploy"

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/supportchat
  K8S_NAMESPACE: dev

jobs:
  # ── 1. Lint & type-check ────────────────────────────────────────────────────
  lint:
    name: Lint & Type-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install & lint backend
        working-directory: backend
        run: |
          npm ci
          npm run typecheck

      - name: Enable Corepack (yarn)
        run: corepack enable

      - name: Install & lint frontend
        working-directory: frontend
        run: |
          yarn install --frozen-lockfile
          yarn lint

  # ── 2. Build & push Docker images ──────────────────────────────────────────
  build-and-push:
    name: Build & Push Images
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      contents: read
      packages: write
    outputs:
      sha_short: ${{ steps.vars.outputs.sha_short }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set image tag vars
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push frontend (dev)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/frontend.dev.Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:dev-${{ steps.vars.outputs.sha_short }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:dev-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push backend (dev)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/backend.dev.Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-backend:dev-${{ steps.vars.outputs.sha_short }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-backend:dev-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── 3. Deploy to k8s/dev ───────────────────────────────────────────────────
  deploy:
    name: Deploy → dev
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: dev
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure kubectl
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBECONFIG_DEV }}

      - name: Substitute image SHA in manifests
        env:
          SHA: ${{ needs.build-and-push.outputs.sha_short }}
        run: |
          sed -i "s|supportchat-frontend:dev-<SHA>|${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:dev-${SHA}|g" k8s/dev/frontend-deployment.yaml
          sed -i "s|supportchat-backend:dev-<SHA>|${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-backend:dev-${SHA}|g"   k8s/dev/backend-deployment.yaml

      - name: Ensure namespace exists
        run: kubectl get namespace ${{ env.K8S_NAMESPACE }} || kubectl create namespace ${{ env.K8S_NAMESPACE }}

      - name: Apply manifests
        run: kubectl apply -f k8s/dev/ --namespace=${{ env.K8S_NAMESPACE }}

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/frontend --namespace=${{ env.K8S_NAMESPACE }} --timeout=120s
          kubectl rollout status deployment/backend  --namespace=${{ env.K8S_NAMESPACE }} --timeout=120s
