apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: prod
  labels:
    app: frontend
    env: prod
spec:
  type: ClusterIP
  selector:
    app: frontend
    env: prod
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
