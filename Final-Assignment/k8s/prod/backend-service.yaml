apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: prod
  labels:
    app: backend
    env: prod
spec:
  type: ClusterIP
  selector:
    app: backend
    env: prod
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
