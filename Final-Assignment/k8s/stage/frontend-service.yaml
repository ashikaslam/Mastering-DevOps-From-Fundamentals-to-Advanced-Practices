apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: stage
  labels:
    app: frontend
    env: stage
spec:
  type: ClusterIP
  selector:
    app: frontend
    env: stage
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
