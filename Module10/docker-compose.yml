services:
  nginx:
    image: nginx:latest
    container_name: nginx_web_server
    ports:
      - "80:80"
    restart: always

  express-app:
    build: ./express-app
    container_name: express_backend_app
    ports:
      - "8080:8080"
    restart: always
    depends_on:
      - nginx
